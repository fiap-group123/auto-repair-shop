# Copy to `.env` or export before running the API. Production must set these.
# Compose sets DATABASE_URL to jdbc:postgresql://postgres:5432/autorepairshop on the api service.
DATABASE_URL=jdbc:postgresql://localhost:5432/autorepairshop
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
JWT_SECRET=change-me-to-a-32-byte-or-longer-secret-key
JWT_TTL_SECONDS=900
REFRESH_TTL_SECONDS=1209600
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_FROM=oficina@localhost
INVITE_BASE_URL=http://localhost:8080/invite
